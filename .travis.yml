language: python
python:
  -"3.6"
  -"3.7"
  -"3.8"
before_install:
  - pip install pytest pytest-cov
  - pip install coveralls
script:
  - py.test .
after-sucess:
- if [[ $ENV == python=3.8* ]]; then
    coveralls;
  fi